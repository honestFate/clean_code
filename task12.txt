2 * self.capacity - self.capacity * GROWTH_FACTOR
// task3/task3.py: двойка стояла в двух методах, полуторка в третьем,
// связь между ними по коду не читалась. Теперь значение связывается
// с именем один раз при загрузке модуля.
// Позже связывать незачем, коэффициенты роста и усечения - это часть
// доказательства амортизированной O(1) для append, задание их в рантайме
// потребовало бы проверки соотношения, иначе массив начнёт чередовать
// resize на границе. Гибкость, которая никому не нужна.


sz=17, stp=3, load_factor=0.75 в сигнатуре
- DEFAULT_TABLE_SIZE, DEFAULT_PROBE_STEP, DEFAULT_LOAD_FACTOR как Final модуля
  плюс параметры конструктора
// task8/task8_2.py: умолчания связываются при загрузке модуля,
// фактические значения - при создании экземпляра.
// Гибкость нужна по требованиям: тесты создают таблицы с разным размером
// и шагом, чтобы проверить коллизии и зацикливание пробирования.
// Нужна проверка в конструкторе assert 0 < load_factor < 1.
// При связывании на этапе написания кода такая проверка не нужна.


self.size - пересчитывается в _resize по факту превышения load factor
self.bases = self._make_bases() - множители зависят от фактического self.size
// task8/task8_2.py, DynamicHashTable и MultiHashTable: размер не известен
// ни при написании кода, ни при создании объекта, его задаёт число
// вставленных элементов.
// _next_prime перебирает кандидатов с проверкой делимости до isqrt(n),
// поэтому вызывается по превышению load factor, а не на каждой
// вставке. При удвоении размера рост случается O(log n) раз на n вставок,
// амортизированная стоимость put остаётся O(1).
// _make_bases по той же причине вызывается один раз в конструкторе,
// а не внутри _candidates на каждом вычислении хэша.

